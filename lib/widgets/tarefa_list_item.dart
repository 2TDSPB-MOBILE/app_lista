import 'package:flutter/material.dart';

class TarefaListItem extends StatelessWidget {
  const TarefaListItem({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.grey[200],
      height: 60,
    );
  }
}